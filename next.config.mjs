/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "xsgames.co",
      "i.pinimg.com",
      "images.unsplash.com",
      "plus.unsplash.com",
    ],
  },
};

export default nextConfig;
